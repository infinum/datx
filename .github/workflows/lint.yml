name: Docs

on:
  push:
    branches: [master, release/*]

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 1

      - name: Install & run
        run: |
          yarn
          yarn lint --quiet
